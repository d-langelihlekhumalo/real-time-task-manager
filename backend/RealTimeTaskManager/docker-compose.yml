version: "3.8"

# This project now uses a single Dockerfile for deployment.
# docker-compose.yml is intentionally minimal here to avoid conflicting with external deploy tools.
services: {}

volumes: {}

networks: {}
